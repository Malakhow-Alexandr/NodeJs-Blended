const { HttpError } = require("../utils/HttpError");
const { User } = require("../models/User");

const signupService = () => {};

const loginService = () => {};

const logoutService = () => {};

module.exports = {
  signupService,
  loginService,
  logoutService,
};
